apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poke-manifests.fullname" . }}-test-connection"
  labels:
    {{- include "poke-manifests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poke-manifests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
